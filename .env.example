# Build-time API origin (no trailing slash required).
# Staging example:
#   VITE_API_BASE_URL=https://staging.example.com/api-2/api npm run build
# Unset → defaults to production https://jdwd40.com/api-2/api
VITE_API_BASE_URL=https://jdwd40.com/api-2/api
