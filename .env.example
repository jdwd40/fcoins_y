# Coins frontend — public runtime config (names only; no secrets).
# The anon/publishable key is intentionally public but must come from env.
VITE_SUPABASE_URL=https://supabase.jdwd40.xyz
VITE_SUPABASE_ANON_KEY=replace-with-staging-or-prod-anon-key
